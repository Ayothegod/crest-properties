---
import "@/styles/globals.css";
import Layout from "../layouts/Layout.astro";
import Action from "@/components/build/Action.tsx";
import { actions } from "astro:actions";
// import { navigate } from "astro:transitions/client";

const { data, error } = await actions.getAllUsers();

if (error) {
  console.log("ERROR");
  // Astro.redirect("/")
  return;
}

console.log(data);
---

<Layout title="Crest properties - Home">
  <div class="bodySize py-20 px-4">
    <p>Listings</p>

    <Action />
  </div>
</Layout>
